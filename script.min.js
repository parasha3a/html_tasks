const menuToggle=document.querySelector('.menu-toggle'),navList=document.querySelector('.nav-list');menuToggle&&navList&&(menuToggle.addEventListener('click',()=>{navList.classList.toggle('active')}),document.querySelectorAll('.nav-link').forEach(link=>{link.addEventListener('click',()=>{navList.classList.remove('active')})}));document.querySelectorAll('.nav-link').forEach(link=>{link.addEventListener('click',e=>{e.preventDefault();const targetId=link.getAttribute('href');targetId.startsWith('#')&&(targetSection=document.querySelector(targetId))&&targetSection.scrollIntoView({behavior:'smooth',block:'start'})})});const contactForm=document.getElementById('contact-form');contactForm&&contactForm.addEventListener('submit',e=>{e.preventDefault();const formData=new FormData(contactForm),name=formData.get('name'),email=formData.get('email'),message=formData.get('message');name&&email&&message&&(modal=document.getElementById('modal'))&&(modal.classList.add('active'),contactForm.reset(),console.log('Форма отправлена:',{name,email,message}))});const modalClose=document.getElementById('modal-close'),modal=document.getElementById('modal');modalClose&&modal&&(modalClose.addEventListener('click',()=>{modal.classList.remove('active')}),modal.addEventListener('click',e=>{e.target===modal&&modal.classList.remove('active')}));const ctaBtn=document.getElementById('cta-btn');ctaBtn&&ctaBtn.addEventListener('click',()=>{const contactSection=document.querySelector('#contact');contactSection&&contactSection.scrollIntoView({behavior:'smooth',block:'start'})});const observerOptions={threshold:.1,rootMargin:'0px 0px -50px 0px'},observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{entry.isIntersecting&&(entry.target.style.opacity='1',entry.target.style.transform='translateY(0)')})},observerOptions);document.querySelectorAll('section').forEach(section=>{section.style.opacity='0',section.style.transform='translateY(20px)',section.style.transition='opacity 0.6s ease, transform 0.6s ease',observer.observe(section)});

