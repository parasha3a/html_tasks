# Инструкция по деплою сайта

Проект успешно собран и готов к деплою! Папка `dist/` содержит оптимизированную версию сайта.

## ✅ Статус сборки

- ✅ Проект собран успешно
- ✅ CSS минифицирован (4.05 kB)
- ✅ JS минифицирован (2.36 kB)
- ✅ HTML оптимизирован (5.20 kB)
- ✅ Конфигурационные файлы созданы

## 🚀 Варианты деплоя

### Вариант 1: Netlify (Рекомендуется - самый простой)

1. **Перейдите на [Netlify](https://www.netlify.com/)**
2. **Войдите или зарегистрируйтесь** (можно через GitHub)
3. **Выберите способ деплоя:**

   **A) Drag & Drop (быстрый способ):**
   - Перетащите папку `dist/` в область "Deploy" на главной странице Netlify
   - Сайт будет опубликован мгновенно
   - Получите ссылку вида: `https://random-name-123.netlify.app`

   **B) Через Git (с автодеплоем):**
   - Нажмите "New site from Git"
   - Выберите GitHub и авторизуйтесь
   - Выберите репозиторий `html_tasks`
   - Настройки (уже настроены в `netlify.toml`):
     - Build command: `npm run build`
     - Publish directory: `dist`
   - Нажмите "Deploy site"
   - При каждом `git push` сайт будет автоматически обновляться

### Вариант 2: Vercel

1. **Перейдите на [Vercel](https://vercel.com/)**
2. **Войдите через GitHub**
3. **Нажмите "Import Project"**
4. **Выберите репозиторий `html_tasks`**
5. **Настройки (уже настроены в `vercel.json`):**
   - Framework Preset: Other
   - Build Command: `npm run build`
   - Output Directory: `dist`
6. **Нажмите "Deploy"**
7. **Сайт будет доступен по адресу:** `https://html-tasks.vercel.app`

### Вариант 3: GitHub Pages

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваш-username/html-tasks.git
   git push -u origin main
   ```

2. **Настройте GitHub Pages:**
   - Откройте репозиторий на GitHub
   - Settings → Pages
   - Source: Deploy from a branch
   - Branch: `main` → `/ (root)`
   - Нажмите Save

3. **Для деплоя из папки dist:**
   - Создайте ветку `gh-pages`
   - Скопируйте содержимое `dist/` в корень ветки
   - В настройках Pages выберите ветку `gh-pages`

## 📋 Быстрая команда для деплоя

Если нужно пересобрать проект перед деплоем:

```bash
npm run build
```

Затем загрузите содержимое папки `dist/` на выбранную платформу.

## 🔍 Проверка после деплоя

После деплоя проверьте:

1. **Откройте сайт в браузере**
2. **Проверьте консоль (F12 → Console)** - не должно быть ошибок
3. **Проверьте адаптивность** (F12 → Toggle Device Toolbar)
4. **Запустите Lighthouse** (F12 → Lighthouse → Generate report)
5. **Проверьте валидность HTML** на [W3C Validator](https://validator.w3.org/)

## 📝 Примечания

- Файлы `netlify.toml` и `vercel.json` уже созданы и настроены
- При деплое через Git изменения применяются автоматически при каждом push
- HTTPS включается автоматически на всех платформах
- Домен можно настроить в настройках проекта на выбранной платформе

## 🆘 Решение проблем

**Если сайт не открывается:**
- Проверьте, что папка `dist/` содержит все файлы
- Убедитесь, что `index.html` находится в корне `dist/`
- Проверьте консоль браузера на ошибки

**Если изображения не загружаются:**
- Убедитесь, что папка `images/` скопирована в `dist/`
- Проверьте пути к изображениям в HTML

**Если стили не применяются:**
- Проверьте, что CSS файлы находятся в `dist/assets/`
- Очистите кеш браузера (Ctrl+Shift+R)

